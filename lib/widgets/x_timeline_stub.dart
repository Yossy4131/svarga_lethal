import 'package:flutter/widgets.dart';

/// Web 以外のプラットフォーム向けスタブ
class XTimeline extends StatelessWidget {
  const XTimeline({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
